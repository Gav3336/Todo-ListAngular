export interface todoCardInterface {
  id: number;
  title: string;
  description: string;
  category_name: string;

  dueDate: string;
  dueTime: string;

  priority: string;
  isCompleted: boolean;
}
